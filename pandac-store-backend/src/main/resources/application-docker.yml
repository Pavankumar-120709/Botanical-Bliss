spring:
  application:
    name: botanical-bliss-backend
  
  # Database configuration for Docker
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://postgres:5432/botanical-bliss}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${JPA_SHOW_URL:false}
    properties:
      hibernate:
        format-sql: ${HIBERNATE_FORMAT_SQL:false}
  
  # Flyway configuration
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration
    table: flyway_schema_history
    
  servlet:
    multipart:
      max-file-size: ${FILE_MAX_SIZE:10MB}
      max-request-size: ${FILE_MAX_REQUEST_SIZE:10MB}

# Application-specific configuration
app:
  file:
    upload-dir: ${FILE_UPLOAD_DIR:uploads/images}
    max-file-size: ${FILE_MAX_SIZE:10MB}
    max-request-size: ${FILE_MAX_REQUEST_SIZE:10MB}
  git:
    repository-url: ${GIT_REPO_URL}
    username: ${GIT_USERNAME}
    token: ${GIT_TOKEN}
    cdn-base-url: ${CDN_BASE_URL}

# Actuator configuration for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# Logging configuration
logging:
  level:
    root: INFO
    '[in.botanicalbliss.store]': INFO
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"